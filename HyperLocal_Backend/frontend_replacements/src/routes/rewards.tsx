
import { createFileRoute } from "@tanstack/react-router"; import { Layout } from "@/components/Layout"; import { useStore } from "@/lib/store"; import { Coins,Gift } from "lucide-react";
export const Route=createFileRoute("/rewards")({component:RewardsPage});
function RewardsPage(){const {coins}=useStore();return <Layout><div className="card-surface overflow-hidden p-8"><div className="flex items-center gap-3"><Coins className="size-8 text-accent"/><div><h1 className="font-display text-3xl font-bold">Rewards</h1><p className="text-sm text-muted-foreground">Your coin balance is stored in the backend account.</p></div></div><div className="mt-8 rounded-2xl bg-secondary p-6"><p className="text-xs uppercase text-muted-foreground">Available coins</p><p className="mt-1 font-display text-4xl font-extrabold">{coins}</p></div></div><div className="mt-6 grid gap-4 md:grid-cols-3">{[["LOCAL100","৳100 off on local seller orders","500 coins"],["FRESH15","15% off grocery, max ৳200","800 coins"],["FREESHIP","Free delivery on any order","300 coins"]].map(([code,a,b])=><div key={code} className="card-surface p-5"><Gift className="size-5 text-primary"/><h2 className="mt-3 font-display font-bold">{a}</h2><p className="text-xs text-muted-foreground">{code} · {b}</p></div>)}</div></Layout>}
