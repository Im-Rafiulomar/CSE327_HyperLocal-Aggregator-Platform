
import { createFileRoute,Link } from "@tanstack/react-router"; import { Layout } from "@/components/Layout"; import { ProductCard } from "@/components/ProductCard"; import { useLang } from "@/lib/i18n"; import { useStore } from "@/lib/store"; import { Heart } from "lucide-react";
export const Route=createFileRoute("/wishlist")({component:WishlistPage});
function WishlistPage(){const {t}=useLang();const {wishlist,products}=useStore();const items=wishlist.map(id=>products.find(p=>p.id===id)).filter(Boolean);return <Layout><h1 className="font-display text-2xl font-bold">{t("wishlist")}</h1><p className="text-sm text-muted-foreground">Saved products are stored in your backend account.</p>{!items.length?<div className="card-surface mt-6 flex flex-col items-center gap-3 p-12 text-center"><Heart className="size-10 text-muted-foreground"/><p>{t("emptyWishlist")}</p><Link to="/search" search={{q:"",category:undefined}} className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Browse products</Link></div>:<div className="mt-4 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{items.map(p=><ProductCard key={p!.id} product={p!}/>)}</div>}</Layout>}
