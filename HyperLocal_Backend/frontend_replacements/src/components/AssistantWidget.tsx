
import { useEffect,useRef,useState } from "react"; import { Link } from "@tanstack/react-router"; import { Bot,Send,X } from "lucide-react"; import { useLang } from "@/lib/i18n";
const API=import.meta.env.VITE_API_URL??"http://localhost:5000/api";
type Msg={role:"user"|"assistant";text:string};
const suggestions=["Compare the two cheapest headphone offers","Where is my order?","Suggest a gift under ৳2000","Are the reviews trustworthy?"];
export function AssistantWidget(){const {t}=useLang();const [open,setOpen]=useState(false);const [input,setInput]=useState("");const [typing,setTyping]=useState(false);const [msgs,setMsgs]=useState<Msg[]>([{role:"assistant",text:"Salam! I'm your HyperLocal assistant. Ask me to compare sellers, track an order, or find something within your budget."}]);const endRef=useRef<HTMLDivElement>(null);useEffect(()=>endRef.current?.scrollIntoView({behavior:"smooth"}),[msgs,typing,open]);const send=async(text:string)=>{if(!text.trim())return;setMsgs(m=>[...m,{role:"user",text}]);setInput("");setTyping(true);try{const r=await fetch(`${API}/ai/assistant`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({message:text})});const b=await r.json();setMsgs(m=>[...m,{role:"assistant",text:b.reply||"I could not generate a response."}])}catch{setMsgs(m=>[...m,{role:"assistant",text:"The backend is unavailable. Start the Express server and try again."}])}finally{setTyping(false)}};return <><button onClick={()=>setOpen(o=>!o)} className="fixed bottom-5 right-5 z-50 flex items-center gap-2 rounded-full bg-hero-gradient px-4 py-3 text-sm font-semibold text-primary-foreground shadow-lift">{open?<X className="size-5"/>:<Bot className="size-5"/>}<span className="hidden sm:inline">{t("assistant")}</span></button>{open&&<div className="fixed bottom-24 right-5 z-50 flex h-[520px] w-[min(92vw,380px)] flex-col overflow-hidden card-surface shadow-lift"><div className="flex items-center gap-2 bg-hero-gradient px-4 py-3 text-primary-foreground"><Bot className="size-5"/><div><div className="text-sm font-semibold">{t("assistant")}</div><div className="text-[11px] opacity-80">Connected to backend service</div></div></div><div className="flex-1 space-y-3 overflow-y-auto p-3">{msgs.map((m,i)=><div key={i} className={m.role==="user"?"flex justify-end":""}><div className={m.role==="user"?"max-w-[85%] rounded-2xl rounded-br-sm bg-primary px-3 py-2 text-sm text-primary-foreground":"max-w-[92%] text-sm leading-relaxed"}>{m.text}</div></div>)}{typing&&<div className="animate-pulse text-sm text-muted-foreground">Thinking…</div>}<div ref={endRef}/></div><div className="flex flex-wrap gap-1.5 border-t px-3 pt-2">{suggestions.map(s=><button key={s} onClick={()=>send(s)} className="rounded-full border px-2.5 py-1 text-[11px] text-muted-foreground hover:bg-secondary">{s}</button>)}</div><form onSubmit={e=>{e.preventDefault();send(input)}} className="flex items-center gap-2 p-3"><input value={input} onChange={e=>setInput(e.target.value)} placeholder="Ask anything…" className="flex-1 rounded-lg border bg-background px-3 py-2 text-sm"/><button className="rounded-lg bg-primary p-2 text-primary-foreground"><Send className="size-4"/></button></form></div>}</>}
